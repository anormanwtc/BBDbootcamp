@model List<Website.Models.Person>;

@{
    ViewData["Title"] = "Person";
    int i = 0;
}

@foreach (Person item in Model)
{
    @if (item != null)
    {
<h2>@ViewData["Title"]</h2>
<hr />
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model[i].Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model[i].Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model[i].Department)
        </dt>
        <dd>
            @Html.DisplayFor(model => model[i].Department)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model[i].Years)
        </dt>
        <dd>
            @Html.DisplayFor(model => model[i].Years)
        </dd>
    </dl>
</div>
    i++;
@* <div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
</div> *@
    }
}